@model IEnumerable<BloodDonationApplication.Models.Korisnik>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KorisnickoIme)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sifra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KorisnickoIme)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sifra)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KorisnikId">Uredi</a> |
                <a asp-action="Details" asp-route-id="@item.KorisnikId">Detalji</a> |
                <a asp-action="Delete" asp-route-id="@item.KorisnikId">Obriši</a>
            </td>
        </tr>
}
    </tbody>
</table>

<br />

<p>
    <a asp-action="Index" asp-controller="Home">Odjava</a>
</p>
